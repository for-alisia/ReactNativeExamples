// Dependencies
import React from 'react';
import { StyleSheet, View } from 'react-native';

const SbCard = () => {
  return <View style={styles.container}>Card Component</View>;
};

const styles = StyleSheet.create({
  container: {},
});

export default SbCard;
